---
title: "stat_ellipse"
output: html_document
---

Creates plot from normal data ellipses computation and convert it into ggplotly

```{r setup, include=FALSE}
library(rvest);library(xml2);library(httr)
library(ggplot2)

knitr::opts_chunk$set(
  class.output  = "bg-light"
)
```

```{r, include=FALSE}
url <- "https://ggplot2.tidyverse.org/reference/stat_ellipse.html"
uastring <- "Mozilla/5.0 (Windows NT 6.1) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/88.0.4324.190 Safari/537.36"
session <- html_session(url, user_agent(uastring))
s <- html_text(html_nodes(session, xpath = "//*[@class='input']"))
s <- unlist(strsplit(s, split = '\n', fixed = TRUE))
```

```{r example1, echo=F}
z <- s[1:3]
z_1 <- paste("p <-", "  \n", paste(z, collapse = '\n'))
p <- eval(parse(text = z))
```

```{r, echo=F, comment=NA}
writeLines(z_1)
write(z_1, "./stat_ellipse/example1.R")
```

```{r}
plotly::ggplotly(p)
```

```{r example2, echo=F}
z <- s[5:7]
z_1 <- paste("p <-", "  \n", paste(z, collapse = '\n'))
p <- eval(parse(text = z))
```

```{r, echo=F, comment=NA}
writeLines(z_1)
write(z_1, "./stat_ellipse/example2.R")
```

```{r}
plotly::ggplotly(p)
```

```{r example3, echo=F}
z <- s[9:12]
z_1 <- paste("p <-", "  \n", paste(z, collapse = '\n'))
p <- eval(parse(text = z))
```

```{r, echo=F, comment=NA}
writeLines(z_1)
write(z_1, "./stat_ellipse/example3.R")
```

```{r}
plotly::ggplotly(p)
```

```{r example4, echo=F}
z <- s[14:18]
z_1 <- paste("p <-", "  \n", paste(z, collapse = '\n'))
p <- eval(parse(text = z))
```

```{r, echo=F, comment=NA}
writeLines(z_1)
write(z_1, "./stat_ellipse/example4.R")
```

```{r}
plotly::ggplotly(p)
```

```{r example5, echo=F}
z <- s[20:21]
z_1 <- paste("p <-", "  \n", paste(z, collapse = '\n'))
p <- eval(parse(text = z))
```

```{r, echo=F, comment=NA}
writeLines(z_1)
write(z_1, "./stat_ellipse/example5.R")
```

```{r}
plotly::ggplotly(p)
```